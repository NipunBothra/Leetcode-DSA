class Solution {
public:
    int findMin(vector<int>& nums) {
        int n=nums.size();
        int minm=nums[0];
        for(int i=1;i<n;i++)
        {
            if(nums[i]<minm)
            minm=nums[i];
        }
        return minm;
    }
};
