class Solution {
public:
    vector<double> convertTemperature(double celsius) {
        vector<double>res;
        double kelvin=celsius+273.15;
        double fah=celsius*1.8 + 32;
        res.insert(res.begin(),kelvin);
        res.insert(res.begin()+1,fah);
        return res;        
    }
};
